export const dynamic = "force-dynamic"

import ScopeForm from "@/components/ScopeForm"

export default function ScopePage() {
  return (
    <main style={{ minHeight: "100vh", backgroundColor: "#F2F4F7" }}>
      <ScopeForm />
    </main>
  )
}
